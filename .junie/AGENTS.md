* Use `uv` for dependency management.
* ignore files in the `main_5` and `non_essential` directories in the project root.
* Prefer `pytest` for tests.
* The project does not use the `pytest-cov` plugin.
* Keep code style compatible with Python 3.13.
* Keep all unit tests for a given Python class in the same file.
* make use of `*.md` files to help you answer questions and write documentation, code, etc. start with the `AGENTS.md`
  file in the project root.

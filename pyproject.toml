[project]
name = "trays"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
dev = [
    "ruff>=0.14.14",
    "pytest>=9.0.2",
    "coverage>=7.13.1",
]
